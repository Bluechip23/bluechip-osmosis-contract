[package]
name = "mock-pyth"
version.workspace = true
edition = "2021"
description = "TEST-ONLY mock of the Pyth CW price-feed contract for the osmosis-test-tube harness"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib", "rlib"]

[features]
library = []

[dependencies]
cosmwasm-schema = { workspace = true }
cosmwasm-std = { workspace = true }
cw-storage-plus = { workspace = true }
